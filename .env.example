# GEMINI_API_KEY: Server-side Gemini API key.
# This is used only by the Express backend endpoint and is never bundled in client JS.
GEMINI_API_KEY="YOUR_GEMINI_API_KEY"

# PORT: Local server port (optional).
PORT="3000"

# APP_URL: Optional app host URL for callbacks/links.
APP_URL="MY_APP_URL"
